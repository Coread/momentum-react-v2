import {CodeEntryDigit} from './';
import {mount} from 'enzyme';
import React from 'react';

describe('CodeEntryDigit', () => {
  describe('snapshot', () => {
    it('should match snapshot', () => {
      /* ...snapshot test... */
    });
  });

  describe('attributes', () => {
    it('should have X value', () => {
      /* ...attribute tests... */
    });
  });
});
