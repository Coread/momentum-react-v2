import {CodeEntry2} from './';
import {mount} from 'enzyme';
import React from 'react';

describe('CodeEntry2', () => {
  describe('snapshot', () => {
    it('should match snapshot', () => {
      /* ...snapshot test... */
    });
  });

  describe('attributes', () => {
    it('should have X value', () => {
      /* ...attribute tests... */
    });
  });
});
