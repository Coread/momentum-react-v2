import {ReactPinInput} from './';
import {mount} from 'enzyme';
import React from 'react';

describe('ReactPinInput', () => {
  describe('snapshot', () => {
    it('should match snapshot', () => {
      /* ...snapshot test... */
    });
  });

  describe('attributes', () => {
    it('should have X value', () => {
      /* ...attribute tests... */
    });
  });
});
